# ============================================================================
# Variables de Entorno - AuriPortal v4
# ============================================================================
# Copiar este archivo a .env y configurar los valores reales
# cp .env.example .env
# nano .env
# ============================================================================
#
# ⚠️ IMPORTANTE: NUNCA commitees el archivo .env con valores reales
# Este archivo (.env.example) es seguro para versionar (solo placeholders)
# ============================================================================

# ============================================================================
# Entorno
# ============================================================================
NODE_ENV=production
APP_ENV=prod
PORT=3000
HOST=0.0.0.0

# ============================================================================
# Base de Datos PostgreSQL
# ============================================================================
PGUSER=<PGUSER>
PGPASSWORD=<PGPASSWORD>
PGHOST=localhost
PGPORT=5432
PGDATABASE=aurelinportal
# O usar DATABASE_URL completo:
# DATABASE_URL=postgresql://<PGUSER>:<PGPASSWORD>@localhost:5432/aurelinportal

# ============================================================================
# APIs Externas - ClickUp (REQUERIDO)
# ============================================================================
# Obtener token desde: https://app.clickup.com → Settings → Apps → API
CLICKUP_API_TOKEN=<CLICKUP_API_TOKEN>
CLICKUP_FOLDER_ID=<CLICKUP_FOLDER_ID>
CLICKUP_TEAM_ID=<CLICKUP_TEAM_ID>
CLICKUP_SPACE_ID=<CLICKUP_SPACE_ID>

# ============================================================================
# APIs Externas - Kajabi (REQUERIDO)
# ============================================================================
# Obtener desde: https://kajabi.com → Settings → API
KAJABI_CLIENT_ID=<KAJABI_CLIENT_ID>
KAJABI_CLIENT_SECRET=<KAJABI_CLIENT_SECRET>

# ============================================================================
# APIs Externas - Typeform (OPCIONAL)
# ============================================================================
# Obtener desde: https://admin.typeform.com → Account → Personal tokens
TYPEFORM_API_TOKEN=<TYPEFORM_API_TOKEN>

# ============================================================================
# APIs Externas - Cloudflare (OPCIONAL)
# ============================================================================
# Opción 1: API Token (Recomendado)
# Obtener desde: https://dash.cloudflare.com/profile/api-tokens
CLOUDFLARE_API_TOKEN=<CLOUDFLARE_API_TOKEN>
# Opción 2: Email + API Key (Alternativa)
CLOUDFLARE_EMAIL=<CLOUDFLARE_EMAIL>
CLOUDFLARE_API_KEY=<CLOUDFLARE_API_KEY>

# ============================================================================
# Google Workspace (OPCIONAL pero recomendado)
# ============================================================================
# Opción 1: Service Account (Recomendado para servidores)
# Obtener desde: https://console.cloud.google.com → APIs & Services → Credentials
GOOGLE_SERVICE_ACCOUNT_KEY=<GOOGLE_SERVICE_ACCOUNT_KEY_JSON>
GOOGLE_SERVICE_ACCOUNT_IMPERSONATE=<GOOGLE_SERVICE_ACCOUNT_IMPERSONATE>
GOOGLE_PROJECT_ID=<GOOGLE_PROJECT_ID>
GOOGLE_WORKSPACE_DOMAIN=<GOOGLE_WORKSPACE_DOMAIN>

# Opción 2: OAuth2 (Para aplicaciones con usuarios)
GOOGLE_CLIENT_ID=<GOOGLE_CLIENT_ID>
GOOGLE_CLIENT_SECRET=<GOOGLE_CLIENT_SECRET>
GOOGLE_REFRESH_TOKEN=<GOOGLE_REFRESH_TOKEN>
GOOGLE_REDIRECT_URI=<GOOGLE_REDIRECT_URI>

# ============================================================================
# Google Drive - Transcripciones (OPCIONAL)
# ============================================================================
GOOGLE_DRIVE_AUDIOS_FOLDER_ID=<GOOGLE_DRIVE_AUDIOS_FOLDER_ID>
GOOGLE_DRIVE_TRANSCRIPCIONES_FOLDER_ID=<GOOGLE_DRIVE_TRANSCRIPCIONES_FOLDER_ID>
GOOGLE_DRIVE_CANALIZACIONES_FOLDER=<GOOGLE_DRIVE_CANALIZACIONES_FOLDER>
DRIVE_MONITOR_INTERVAL=300000
DRIVE_WEBHOOK_AUTO_SETUP=true
WEBHOOK_BASE_URL=<WEBHOOK_BASE_URL>
SERVER_URL=<SERVER_URL>

# ============================================================================
# Google Apps Script Worker (OPCIONAL)
# ============================================================================
# Obtener URL después de desplegar el proyecto en Google Apps Script
GOOGLE_WORKER_URL=<GOOGLE_WORKER_URL>
# Generar con: openssl rand -hex 32
GOOGLE_WORKER_SECRET=<GOOGLE_WORKER_SECRET>

# ============================================================================
# SSH Servidor "dani" - Whisper (OPCIONAL)
# ============================================================================
SSH_DANI_HOST=<SSH_DANI_HOST>
SSH_DANI_TAILSCALE_HOST=<SSH_DANI_TAILSCALE_HOST>
SSH_DANI_PORT=22
SSH_DANI_USER=<SSH_DANI_USER>
SSH_DANI_KEY_PATH=<SSH_DANI_KEY_PATH>
SSH_DANI_INPUT_PATH=<SSH_DANI_INPUT_PATH>
SSH_DANI_OUTPUT_PATH=<SSH_DANI_OUTPUT_PATH>
SSH_DANI_PROYECTO_PATH=<SSH_DANI_PROYECTO_PATH>
SSH_DANI_ENTORNO_VIRTUAL=<SSH_DANI_ENTORNO_VIRTUAL>
SSH_DANI_MODELO_WHISPER=base
SSH_DANI_IDIOMA=es
SSH_DANI_FORMATO=json

# ============================================================================
# Seguridad
# ============================================================================
# Generar con: openssl rand -hex 32
# O con Node.js: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
COOKIE_SECRET=<COOKIE_SECRET>
ADMIN_PASSWORD=<ADMIN_PASSWORD>

# ============================================================================
# Email
# ============================================================================
EMAIL_FROM=noreply@portal.pdeeugenihidalgo.org

# ============================================================================
# Zoom (OPCIONAL)
# ============================================================================
ZOOM_ACCOUNT_ID=<ZOOM_ACCOUNT_ID>
ZOOM_CLIENT_ID=<ZOOM_CLIENT_ID>
ZOOM_CLIENT_SECRET=<ZOOM_CLIENT_SECRET>

# ============================================================================
# NOTAS IMPORTANTES
# ============================================================================
#
# 1. NUNCA commitees este archivo con valores reales
# 2. El archivo .env real debe estar en .gitignore (ya configurado)
# 3. Usa valores diferentes para desarrollo, beta y producción
# 4. Rota los tokens periódicamente
# 5. Usa permisos mínimos necesarios en cada servicio
# 6. Guarda los tokens de forma segura (gestor de contraseñas)
#
# Para más información, ver: CONFIGURACION_TOKENS.md
# ============================================================================
